import 'package:flutter/material.dart';

class AppColors {
  // Primary palette (from mockup)
  static const Color primary = Color(0xFF0B3B5B);
  static const Color primaryLight = Color(0xFF1A5276);

  // Background colors
  static const Color backgroundLight = Color(0xFFF6F7F8);
  static const Color backgroundDark = Color(0xFF111B21);

  // Card / Surface colors
  static const Color cardLight = Colors.white;
  static const Color cardDark = Color(0xFF1C2226);

  // Accent / legacy aliases
  static const Color navy = primary;
  static const Color navyDark = backgroundDark;
  static const Color accent = Color(0xFF2AA4F4);
  static const Color background = backgroundLight;

  // Status colors
  static const Color success = Color(0xFF22C55E);
  static const Color warning = Color(0xFFEAB308);
  static const Color danger = Color(0xFFEF4444);
}
